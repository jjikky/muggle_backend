extends layout

block content
    .form-container
        form(action=`/api/nickname-check`, method="post")
            input(type="string", placeholder="nickname", name="nickname",required=true)
            input(type="submit", value="check")