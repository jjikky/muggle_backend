extends layout

block content
    .form-container
        form(action=`/taste/read-comment`, method="post")
            input(type="string", placeholder="login_uid", name="login_uid",required=true)
            input(type="string", placeholder="taste id", name="taste_id",required=true)
            input(type="string", placeholder="choice", name="choice",required=true)
            input(type="string", placeholder="last_comment_id", name="last_comment_id")
            input(type="submit", value="submit") 