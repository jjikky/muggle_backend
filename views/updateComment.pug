extends layout

block content
    .form-container
        form(action="/taste/update-comment", method ="post")
            input(type="string", name="text", placeholder="text",required=true)
            input(type="string", name="comment_id", placeholder="comment_id",required=true)
            input(type="submit", value="submit") 